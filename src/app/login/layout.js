export const metadata = {
  title: 'Future School System | Okara',
  description: 'Secure login portal for Future School Students, Teachers, and Admin',
};

export default function LoginLayout({ children }) {
  return (
      <>
      {children}
    </>
  );
}